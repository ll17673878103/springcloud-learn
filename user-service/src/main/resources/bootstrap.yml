spring:
  application:
    name: user-service

  config:
    import: optional:nacos:${spring.application.name}-${spring.profiles.active}

  profiles:
    active: dev

  cloud:
    nacos:
      server-addr: 101.43.103.32:8848
      # 这里是在nacos注册中心里注册服务
      discovery:
        namespace: public
     # nacos下配置config会让服务使用nacos的配置文件，反之用本地服务自己的application
      config:
        namespace: public
        file-extension: yaml